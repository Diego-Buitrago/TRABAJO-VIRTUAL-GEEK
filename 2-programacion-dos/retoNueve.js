'use strict';

const name = "Maritza Rueda Hincapie";

const pElement = document.querySelector('p');

pElement.innerHTML = `El nombre de mi compañera es ${name} y esta compuesto por ${name.length} caracteres`